language: java
sudo: required

branches:
  only:
  - master

matrix:
  include:
    - os: linux
      dist: trusty
      jdk: oraclejdk9
    - os: osx
      env: OSX=10.12.1
      osx_image: xcode9.2

script:
  - echo "Starting script"
  - java -version
#  - chmod +x ./gradlew
#  - chmod +x gradlew
  - ./gradlew clean build
